include "std.zod"

i64 fib(i64 n) {
    if n == 0 || n == 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

i64 main() {
    puts("The 10th fibonacci number is: ");
    writed(fib(10));
    exit(0);
}
